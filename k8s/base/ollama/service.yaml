apiVersion: v1
kind: Service
metadata:
  name: ollama
  namespace: homie-prod
spec:
  selector:
    app: ollama
  ports:
    - port: 11434
      targetPort: 11434
  # ClusterIP only — not exposed externally
